# Written by tools/boost-ports.py. Do not edit: run the script again.
#
# One job per Boost library, each waiting for the libraries it is built on,
# in the order Boost's own dependency graph gives. A job apiece says which
# library broke rather than which build did; the waiting means a broken leaf
# skips what is above it instead of failing it a hundred and fifty times.
#
# Every job builds its library and everything underneath it from the
# repositories. What is shared between them is ccache, under one key for all
# of them, because most of what any of these jobs compiles is the libraries
# below it.
name: boost

on:
  push:
    paths:
      - 'registry/boost*/**'
      - 'cmake-everywhere.cmake'
      - '.github/workflows/boost.yml'
  workflow_dispatch:

jobs:
  boost-system:
    needs: [boost-assert, boost-compat, boost-config, boost-throw-exception, boost-variant2, boost-winapi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-system-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-system-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-system-
            boost-ccache-
      - name: boost_system and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_system \
            -DCME_PORT_TARGETS=Boost::system \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-multi-array:
    needs: [boost-array, boost-assert, boost-concept-check, boost-config, boost-core, boost-functional, boost-iterator, boost-mpl, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-multi-array-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-multi-array-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-multi-array-
            boost-ccache-
      - name: boost_multi_array and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_multi_array \
            -DCME_PORT_TARGETS=Boost::multi_array \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-math:
    needs: [boost-assert, boost-concept-check, boost-config, boost-core, boost-integer, boost-lexical-cast, boost-predef, boost-random, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-math-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-math-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-math-
            boost-ccache-
      - name: boost_math and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_math \
            -DCME_PORT_TARGETS=Boost::math \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-smart-ptr:
    needs: [boost-assert, boost-config, boost-core, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-smart-ptr-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-smart-ptr-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-smart-ptr-
            boost-ccache-
      - name: boost_smart_ptr and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_smart_ptr \
            -DCME_PORT_TARGETS=Boost::smart_ptr \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-parameter:
    needs: [boost-config, boost-core, boost-function, boost-fusion, boost-mp11, boost-mpl, boost-optional, boost-preprocessor, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-parameter-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-parameter-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-parameter-
            boost-ccache-
      - name: boost_parameter and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_parameter \
            -DCME_PORT_TARGETS=Boost::parameter \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-algorithm:
    needs: [boost-array, boost-assert, boost-bind, boost-concept-check, boost-config, boost-core, boost-exception, boost-function, boost-iterator, boost-mpl, boost-range, boost-regex, boost-throw-exception, boost-tuple, boost-type-traits, boost-unordered]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-algorithm-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-algorithm-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-algorithm-
            boost-ccache-
      - name: boost_algorithm and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_algorithm \
            -DCME_PORT_TARGETS=Boost::algorithm \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-any:
    needs: [boost-config, boost-throw-exception, boost-type-index]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-any-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-any-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-any-
            boost-ccache-
      - name: boost_any and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_any \
            -DCME_PORT_TARGETS=Boost::any \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-concept-check:
    needs: [boost-config, boost-preprocessor, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-concept-check-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-concept-check-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-concept-check-
            boost-ccache-
      - name: boost_concept_check and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_concept_check \
            -DCME_PORT_TARGETS=Boost::concept_check \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-python:
    needs: [boost-align, boost-bind, boost-config, boost-conversion, boost-core, boost-detail, boost-foreach, boost-function, boost-graph, boost-integer, boost-iterator, boost-lexical-cast, boost-mpl, boost-numeric-conversion, boost-preprocessor, boost-property-map, boost-smart-ptr, boost-tuple, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-python-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-python-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-python-
            boost-ccache-
      - name: boost_python and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_python \
            -DCME_PORT_TARGETS=Boost::python \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-tti:
    needs: [boost-config, boost-function-types, boost-mpl, boost-preprocessor, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-tti-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-tti-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-tti-
            boost-ccache-
      - name: boost_tti and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_tti \
            -DCME_PORT_TARGETS=Boost::tti \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-functional:
    needs: [boost-config, boost-core, boost-function, boost-function-types, boost-mpl, boost-preprocessor, boost-type-traits, boost-typeof, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-functional-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-functional-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-functional-
            boost-ccache-
      - name: boost_functional and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_functional \
            -DCME_PORT_TARGETS=Boost::functional \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-config:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-config-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-config-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-config-
            boost-ccache-
      - name: boost_config and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_config \
            -DCME_PORT_TARGETS=Boost::config \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-log:
    needs: [boost-align, boost-asio, boost-assert, boost-atomic, boost-bind, boost-config, boost-core, boost-date-time, boost-exception, boost-filesystem, boost-function-types, boost-fusion, boost-interprocess, boost-intrusive, boost-io, boost-iterator, boost-move, boost-mpl, boost-optional, boost-parameter, boost-phoenix, boost-predef, boost-preprocessor, boost-property-tree, boost-proto, boost-range, boost-regex, boost-smart-ptr, boost-spirit, boost-system, boost-thread, boost-throw-exception, boost-type-index, boost-type-traits, boost-utility, boost-winapi, boost-xpressive]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-log-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-log-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-log-
            boost-ccache-
      - name: boost_log and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_log \
            -DCME_PORT_TARGETS=Boost::log \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-interprocess:
    needs: [boost-assert, boost-config, boost-container, boost-intrusive, boost-move, boost-winapi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-interprocess-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-interprocess-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-interprocess-
            boost-ccache-
      - name: boost_interprocess and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_interprocess \
            -DCME_PORT_TARGETS=Boost::interprocess \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-exception:
    needs: [boost-assert, boost-config, boost-core, boost-smart-ptr, boost-throw-exception, boost-tuple, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-exception-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-exception-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-exception-
            boost-ccache-
      - name: boost_exception and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_exception \
            -DCME_PORT_TARGETS=Boost::exception \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-foreach:
    needs: [boost-config, boost-core, boost-iterator, boost-mpl, boost-range, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-foreach-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-foreach-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-foreach-
            boost-ccache-
      - name: boost_foreach and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_foreach \
            -DCME_PORT_TARGETS=Boost::foreach \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-spirit:
    needs: [boost-array, boost-assert, boost-config, boost-core, boost-endian, boost-function, boost-function-types, boost-fusion, boost-integer, boost-io, boost-iterator, boost-move, boost-mpl, boost-optional, boost-phoenix, boost-pool, boost-preprocessor, boost-proto, boost-range, boost-smart-ptr, boost-thread, boost-throw-exception, boost-type-traits, boost-typeof, boost-unordered, boost-utility, boost-variant]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-spirit-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-spirit-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-spirit-
            boost-ccache-
      - name: boost_spirit and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_spirit \
            -DCME_PORT_TARGETS=Boost::spirit \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-io:
    needs: [boost-config]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-io-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-io-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-io-
            boost-ccache-
      - name: boost_io and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_io \
            -DCME_PORT_TARGETS=Boost::io \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-units:
    needs: [boost-assert, boost-config, boost-core, boost-integer, boost-io, boost-lambda, boost-math, boost-mpl, boost-preprocessor, boost-type-traits, boost-typeof]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-units-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-units-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-units-
            boost-ccache-
      - name: boost_units and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_units \
            -DCME_PORT_TARGETS=Boost::units \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-preprocessor:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-preprocessor-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-preprocessor-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-preprocessor-
            boost-ccache-
      - name: boost_preprocessor and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_preprocessor \
            -DCME_PORT_TARGETS=Boost::preprocessor \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-format:
    needs: [boost-assert, boost-config, boost-core, boost-optional, boost-smart-ptr, boost-throw-exception, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-format-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-format-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-format-
            boost-ccache-
      - name: boost_format and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_format \
            -DCME_PORT_TARGETS=Boost::format \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-xpressive:
    needs: [boost-assert, boost-config, boost-conversion, boost-core, boost-exception, boost-fusion, boost-integer, boost-iterator, boost-lexical-cast, boost-mpl, boost-numeric-conversion, boost-optional, boost-preprocessor, boost-proto, boost-range, boost-smart-ptr, boost-throw-exception, boost-type-traits, boost-typeof, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-xpressive-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-xpressive-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-xpressive-
            boost-ccache-
      - name: boost_xpressive and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_xpressive \
            -DCME_PORT_TARGETS=Boost::xpressive \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-integer:
    needs: [boost-assert, boost-config, boost-core, boost-throw-exception, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-integer-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-integer-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-integer-
            boost-ccache-
      - name: boost_integer and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_integer \
            -DCME_PORT_TARGETS=Boost::integer \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-thread:
    needs: [boost-assert, boost-atomic, boost-bind, boost-chrono, boost-concept-check, boost-config, boost-container, boost-container-hash, boost-core, boost-date-time, boost-exception, boost-function, boost-io, boost-move, boost-optional, boost-predef, boost-preprocessor, boost-smart-ptr, boost-system, boost-throw-exception, boost-tuple, boost-type-traits, boost-utility, boost-winapi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-thread-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-thread-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-thread-
            boost-ccache-
      - name: boost_thread and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_thread \
            -DCME_PORT_TARGETS=Boost::thread \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-tokenizer:
    needs: [boost-assert, boost-config, boost-iterator, boost-mpl, boost-throw-exception, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-tokenizer-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-tokenizer-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-tokenizer-
            boost-ccache-
      - name: boost_tokenizer and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_tokenizer \
            -DCME_PORT_TARGETS=Boost::tokenizer \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-timer:
    needs: [boost-config, boost-io, boost-predef]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-timer-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-timer-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-timer-
            boost-ccache-
      - name: boost_timer and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_timer \
            -DCME_PORT_TARGETS=Boost::timer \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-regex:
    needs: [boost-assert, boost-config, boost-predef, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-regex-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-regex-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-regex-
            boost-ccache-
      - name: boost_regex and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_regex \
            -DCME_PORT_TARGETS=Boost::regex \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-crc:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-crc-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-crc-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-crc-
            boost-ccache-
      - name: boost_crc and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_crc \
            -DCME_PORT_TARGETS=Boost::crc \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-random:
    needs: [boost-assert, boost-config, boost-core, boost-dynamic-bitset, boost-integer, boost-io, boost-system, boost-throw-exception, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-random-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-random-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-random-
            boost-ccache-
      - name: boost_random and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_random \
            -DCME_PORT_TARGETS=Boost::random \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-serialization:
    needs: [boost-array, boost-assert, boost-config, boost-core, boost-detail, boost-function, boost-integer, boost-io, boost-iterator, boost-move, boost-mpl, boost-optional, boost-predef, boost-preprocessor, boost-smart-ptr, boost-spirit, boost-type-traits, boost-unordered, boost-utility, boost-variant]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-serialization-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-serialization-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-serialization-
            boost-ccache-
      - name: boost_serialization and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_serialization \
            -DCME_PORT_TARGETS=Boost::serialization \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-test:
    needs: [boost-algorithm, boost-assert, boost-bind, boost-config, boost-core, boost-describe, boost-detail, boost-exception, boost-function, boost-io, boost-iterator, boost-mpl, boost-numeric-conversion, boost-optional, boost-preprocessor, boost-smart-ptr, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-test-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-test-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-test-
            boost-ccache-
      - name: boost_test and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_test \
            -DCME_PORT_TARGETS=Boost::unit_test_framework \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-date-time:
    needs: [boost-algorithm, boost-assert, boost-config, boost-core, boost-io, boost-lexical-cast, boost-numeric-conversion, boost-range, boost-smart-ptr, boost-throw-exception, boost-tokenizer, boost-type-traits, boost-utility, boost-winapi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-date-time-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-date-time-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-date-time-
            boost-ccache-
      - name: boost_date_time and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_date_time \
            -DCME_PORT_TARGETS=Boost::date_time \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-logic:
    needs: [boost-config, boost-core]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-logic-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-logic-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-logic-
            boost-ccache-
      - name: boost_logic and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_logic \
            -DCME_PORT_TARGETS=Boost::logic \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-graph:
    needs: [boost-algorithm, boost-any, boost-array, boost-assert, boost-bimap, boost-concept-check, boost-config, boost-container-hash, boost-conversion, boost-core, boost-detail, boost-foreach, boost-function, boost-integer, boost-iterator, boost-lexical-cast, boost-math, boost-move, boost-mpl, boost-multi-index, boost-multiprecision, boost-optional, boost-parameter, boost-preprocessor, boost-property-map, boost-property-tree, boost-random, boost-range, boost-regex, boost-serialization, boost-smart-ptr, boost-spirit, boost-throw-exception, boost-tti, boost-tuple, boost-type-traits, boost-typeof, boost-unordered, boost-utility, boost-xpressive]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-graph-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-graph-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-graph-
            boost-ccache-
      - name: boost_graph and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_graph \
            -DCME_PORT_TARGETS=Boost::graph \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-numeric-conversion:
    needs: [boost-config, boost-conversion, boost-core, boost-mpl, boost-preprocessor, boost-throw-exception, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-numeric-conversion-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-numeric-conversion-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-numeric-conversion-
            boost-ccache-
      - name: boost_numeric_conversion and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_numeric_conversion \
            -DCME_PORT_TARGETS=Boost::numeric_conversion \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-lambda:
    needs: [boost-config, boost-core, boost-detail, boost-iterator, boost-mpl, boost-preprocessor, boost-tuple, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-lambda-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-lambda-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-lambda-
            boost-ccache-
      - name: boost_lambda and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_lambda \
            -DCME_PORT_TARGETS=Boost::lambda \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-mpl:
    needs: [boost-config, boost-core, boost-predef, boost-preprocessor, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-mpl-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-mpl-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-mpl-
            boost-ccache-
      - name: boost_mpl and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_mpl \
            -DCME_PORT_TARGETS=Boost::mpl \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-typeof:
    needs: [boost-config]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-typeof-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-typeof-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-typeof-
            boost-ccache-
      - name: boost_typeof and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_typeof \
            -DCME_PORT_TARGETS=Boost::typeof \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-tuple:
    needs: [boost-config, boost-core, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-tuple-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-tuple-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-tuple-
            boost-ccache-
      - name: boost_tuple and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_tuple \
            -DCME_PORT_TARGETS=Boost::tuple \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-utility:
    needs: [boost-config, boost-core, boost-io, boost-preprocessor, boost-throw-exception, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-utility-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-utility-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-utility-
            boost-ccache-
      - name: boost_utility and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_utility \
            -DCME_PORT_TARGETS=Boost::utility \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-dynamic-bitset:
    needs: [boost-assert, boost-config, boost-container-hash, boost-core, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-dynamic-bitset-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-dynamic-bitset-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-dynamic-bitset-
            boost-ccache-
      - name: boost_dynamic_bitset and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_dynamic_bitset \
            -DCME_PORT_TARGETS=Boost::dynamic_bitset \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-assign:
    needs: [boost-array, boost-config, boost-core, boost-move, boost-mpl, boost-preprocessor, boost-ptr-container, boost-range, boost-throw-exception, boost-tuple, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-assign-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-assign-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-assign-
            boost-ccache-
      - name: boost_assign and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_assign \
            -DCME_PORT_TARGETS=Boost::assign \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-filesystem:
    needs: [boost-assert, boost-atomic, boost-config, boost-container-hash, boost-core, boost-detail, boost-io, boost-iterator, boost-predef, boost-scope, boost-smart-ptr, boost-system, boost-type-traits, boost-winapi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-filesystem-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-filesystem-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-filesystem-
            boost-ccache-
      - name: boost_filesystem and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_filesystem \
            -DCME_PORT_TARGETS=Boost::filesystem \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-function:
    needs: [boost-assert, boost-bind, boost-config, boost-core, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-function-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-function-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-function-
            boost-ccache-
      - name: boost_function and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_function \
            -DCME_PORT_TARGETS=Boost::function \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-conversion:
    needs: [boost-assert, boost-config, boost-smart-ptr, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-conversion-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-conversion-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-conversion-
            boost-ccache-
      - name: boost_conversion and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_conversion \
            -DCME_PORT_TARGETS=Boost::conversion \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-optional:
    needs: [boost-assert, boost-config, boost-core, boost-throw-exception, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-optional-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-optional-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-optional-
            boost-ccache-
      - name: boost_optional and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_optional \
            -DCME_PORT_TARGETS=Boost::optional \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-property-tree:
    needs: [boost-any, boost-assert, boost-bind, boost-config, boost-core, boost-iterator, boost-mpl, boost-multi-index, boost-optional, boost-range, boost-serialization, boost-throw-exception, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-property-tree-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-property-tree-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-property-tree-
            boost-ccache-
      - name: boost_property_tree and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_property_tree \
            -DCME_PORT_TARGETS=Boost::property_tree \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-bimap:
    needs: [boost-concept-check, boost-config, boost-container-hash, boost-core, boost-iterator, boost-lambda, boost-mpl, boost-multi-index, boost-preprocessor, boost-throw-exception, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-bimap-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-bimap-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-bimap-
            boost-ccache-
      - name: boost_bimap and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_bimap \
            -DCME_PORT_TARGETS=Boost::bimap \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-variant:
    needs: [boost-assert, boost-config, boost-container-hash, boost-core, boost-detail, boost-integer, boost-mpl, boost-preprocessor, boost-throw-exception, boost-type-index, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-variant-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-variant-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-variant-
            boost-ccache-
      - name: boost_variant and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_variant \
            -DCME_PORT_TARGETS=Boost::variant \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-array:
    needs: [boost-assert, boost-config, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-array-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-array-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-array-
            boost-ccache-
      - name: boost_array and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_array \
            -DCME_PORT_TARGETS=Boost::array \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-iostreams:
    needs: [boost-assert, boost-config, boost-core, boost-detail, boost-function, boost-integer, boost-iterator, boost-mpl, boost-numeric-conversion, boost-preprocessor, boost-random, boost-range, boost-regex, boost-smart-ptr, boost-throw-exception, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-iostreams-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-iostreams-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-iostreams-
            boost-ccache-
      - name: boost_iostreams and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_iostreams \
            -DCME_PORT_TARGETS=Boost::iostreams \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-multi-index:
    needs: [boost-assert, boost-bind, boost-config, boost-container-hash, boost-core, boost-integer, boost-mp11, boost-preprocessor, boost-smart-ptr, boost-throw-exception, boost-tuple, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-multi-index-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-multi-index-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-multi-index-
            boost-ccache-
      - name: boost_multi_index and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_multi_index \
            -DCME_PORT_TARGETS=Boost::multi_index \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-ptr-container:
    needs: [boost-array, boost-assert, boost-circular-buffer, boost-config, boost-core, boost-iterator, boost-mpl, boost-range, boost-smart-ptr, boost-type-traits, boost-unordered, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-ptr-container-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-ptr-container-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-ptr-container-
            boost-ccache-
      - name: boost_ptr_container and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_ptr_container \
            -DCME_PORT_TARGETS=Boost::ptr_container \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-statechart:
    needs: [boost-assert, boost-bind, boost-config, boost-conversion, boost-core, boost-detail, boost-function, boost-mpl, boost-smart-ptr, boost-thread, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-statechart-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-statechart-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-statechart-
            boost-ccache-
      - name: boost_statechart and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_statechart \
            -DCME_PORT_TARGETS=Boost::statechart \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-static-assert:
    needs: [boost-config]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-static-assert-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-static-assert-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-static-assert-
            boost-ccache-
      - name: boost_static_assert and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_static_assert \
            -DCME_PORT_TARGETS=Boost::static_assert \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-range:
    needs: [boost-array, boost-assert, boost-concept-check, boost-config, boost-container-hash, boost-conversion, boost-core, boost-detail, boost-iterator, boost-mpl, boost-optional, boost-preprocessor, boost-regex, boost-tuple, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-range-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-range-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-range-
            boost-ccache-
      - name: boost_range and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_range \
            -DCME_PORT_TARGETS=Boost::range \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-rational:
    needs: [boost-assert, boost-config, boost-core, boost-integer, boost-throw-exception, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-rational-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-rational-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-rational-
            boost-ccache-
      - name: boost_rational and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_rational \
            -DCME_PORT_TARGETS=Boost::rational \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-iterator:
    needs: [boost-assert, boost-concept-check, boost-config, boost-core, boost-detail, boost-fusion, boost-mpl, boost-optional, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-iterator-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-iterator-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-iterator-
            boost-ccache-
      - name: boost_iterator and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_iterator \
            -DCME_PORT_TARGETS=Boost::iterator \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-graph-parallel:
    needs: [boost-assert, boost-concept-check, boost-config, boost-container-hash, boost-core, boost-detail, boost-dynamic-bitset, boost-filesystem, boost-foreach, boost-function, boost-graph, boost-iterator, boost-lexical-cast, boost-mpi, boost-mpl, boost-optional, boost-property-map, boost-property-map-parallel, boost-random, boost-serialization, boost-smart-ptr, boost-tuple, boost-type-traits, boost-variant]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-graph-parallel-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-graph-parallel-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-graph-parallel-
            boost-ccache-
      - name: boost_graph_parallel and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_graph_parallel \
            -DCME_PORT_TARGETS=Boost::graph_parallel \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-property-map:
    needs: [boost-any, boost-assert, boost-concept-check, boost-config, boost-core, boost-function, boost-iterator, boost-lexical-cast, boost-mpl, boost-smart-ptr, boost-throw-exception, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-property-map-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-property-map-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-property-map-
            boost-ccache-
      - name: boost_property_map and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_property_map \
            -DCME_PORT_TARGETS=Boost::property_map \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-program-options:
    needs: [boost-any, boost-bind, boost-config, boost-core, boost-detail, boost-function, boost-iterator, boost-lexical-cast, boost-smart-ptr, boost-throw-exception, boost-tokenizer, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-program-options-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-program-options-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-program-options-
            boost-ccache-
      - name: boost_program_options and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_program_options \
            -DCME_PORT_TARGETS=Boost::program_options \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-detail:
    needs: [boost-config, boost-core, boost-preprocessor, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-detail-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-detail-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-detail-
            boost-ccache-
      - name: boost_detail and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_detail \
            -DCME_PORT_TARGETS=Boost::detail \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-numeric-interval:
    needs: [boost-config, boost-detail, boost-logic]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-numeric-interval-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-numeric-interval-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-numeric-interval-
            boost-ccache-
      - name: boost_numeric_interval and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_numeric_interval \
            -DCME_PORT_TARGETS=Boost::numeric_interval \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-numeric-ublas:
    needs: [boost-compute, boost-concept-check, boost-config, boost-core, boost-iterator, boost-mpl, boost-numeric-interval, boost-range, boost-serialization, boost-smart-ptr, boost-type-traits, boost-typeof]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-numeric-ublas-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-numeric-ublas-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-numeric-ublas-
            boost-ccache-
      - name: boost_numeric_ublas and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_numeric_ublas \
            -DCME_PORT_TARGETS=Boost::numeric_ublas \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-wave:
    needs: [boost-assert, boost-concept-check, boost-config, boost-core, boost-filesystem, boost-format, boost-iterator, boost-lexical-cast, boost-mpl, boost-multi-index, boost-optional, boost-pool, boost-preprocessor, boost-serialization, boost-smart-ptr, boost-spirit, boost-throw-exception, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-wave-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-wave-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-wave-
            boost-ccache-
      - name: boost_wave and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_wave \
            -DCME_PORT_TARGETS=Boost::wave \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-type-traits:
    needs: [boost-config]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-type-traits-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-type-traits-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-type-traits-
            boost-ccache-
      - name: boost_type_traits and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_type_traits \
            -DCME_PORT_TARGETS=Boost::type_traits \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-bind:
    needs: [boost-config, boost-core]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-bind-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-bind-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-bind-
            boost-ccache-
      - name: boost_bind and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_bind \
            -DCME_PORT_TARGETS=Boost::bind \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-pool:
    needs: [boost-assert, boost-config, boost-integer, boost-throw-exception, boost-type-traits, boost-winapi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-pool-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-pool-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-pool-
            boost-ccache-
      - name: boost_pool and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_pool \
            -DCME_PORT_TARGETS=Boost::pool \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-proto:
    needs: [boost-config, boost-core, boost-fusion, boost-mpl, boost-preprocessor, boost-range, boost-type-traits, boost-typeof, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-proto-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-proto-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-proto-
            boost-ccache-
      - name: boost_proto and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_proto \
            -DCME_PORT_TARGETS=Boost::proto \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-fusion:
    needs: [boost-config, boost-container-hash, boost-core, boost-function-types, boost-mpl, boost-preprocessor, boost-tuple, boost-type-traits, boost-typeof, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-fusion-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-fusion-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-fusion-
            boost-ccache-
      - name: boost_fusion and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_fusion \
            -DCME_PORT_TARGETS=Boost::fusion \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-function-types:
    needs: [boost-config, boost-core, boost-detail, boost-mpl, boost-preprocessor, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-function-types-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-function-types-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-function-types-
            boost-ccache-
      - name: boost_function_types and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_function_types \
            -DCME_PORT_TARGETS=Boost::function_types \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-gil:
    needs: [boost-assert, boost-concept-check, boost-config, boost-container-hash, boost-core, boost-filesystem, boost-headers, boost-integer, boost-iterator, boost-mp11, boost-preprocessor, boost-type-traits, boost-variant2]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-gil-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-gil-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-gil-
            boost-ccache-
      - name: boost_gil and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_gil \
            -DCME_PORT_TARGETS=Boost::gil \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-intrusive:
    needs: [boost-assert, boost-config, boost-move]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-intrusive-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-intrusive-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-intrusive-
            boost-ccache-
      - name: boost_intrusive and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_intrusive \
            -DCME_PORT_TARGETS=Boost::intrusive \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-asio:
    needs: [boost-align, boost-assert, boost-config, boost-context, boost-date-time, boost-system, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-asio-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-asio-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-asio-
            boost-ccache-
      - name: boost_asio and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_asio \
            -DCME_PORT_TARGETS=Boost::asio \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-uuid:
    needs: [boost-assert, boost-config, boost-throw-exception, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-uuid-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-uuid-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-uuid-
            boost-ccache-
      - name: boost_uuid and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_uuid \
            -DCME_PORT_TARGETS=Boost::uuid \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-circular-buffer:
    needs: [boost-assert, boost-concept-check, boost-config, boost-core, boost-move, boost-throw-exception, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-circular-buffer-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-circular-buffer-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-circular-buffer-
            boost-ccache-
      - name: boost_circular_buffer and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_circular_buffer \
            -DCME_PORT_TARGETS=Boost::circular_buffer \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-mpi:
    needs: [boost-assert, boost-config, boost-core, boost-foreach, boost-function, boost-graph, boost-integer, boost-iterator, boost-lexical-cast, boost-mpl, boost-optional, boost-serialization, boost-smart-ptr, boost-throw-exception, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-mpi-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-mpi-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-mpi-
            boost-ccache-
      - name: boost_mpi and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_mpi \
            -DCME_PORT_TARGETS=Boost::mpi \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-unordered:
    needs: [boost-assert, boost-config, boost-container-hash, boost-core, boost-mp11, boost-predef, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-unordered-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-unordered-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-unordered-
            boost-ccache-
      - name: boost_unordered and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_unordered \
            -DCME_PORT_TARGETS=Boost::unordered \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-signals2:
    needs: [boost-assert, boost-bind, boost-config, boost-core, boost-function, boost-iterator, boost-move, boost-mpl, boost-optional, boost-parameter, boost-preprocessor, boost-smart-ptr, boost-throw-exception, boost-tuple, boost-type-traits, boost-variant]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-signals2-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-signals2-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-signals2-
            boost-ccache-
      - name: boost_signals2 and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_signals2 \
            -DCME_PORT_TARGETS=Boost::signals2 \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-accumulators:
    needs: [boost-array, boost-assert, boost-circular-buffer, boost-concept-check, boost-config, boost-core, boost-fusion, boost-iterator, boost-mpl, boost-numeric-conversion, boost-numeric-ublas, boost-parameter, boost-preprocessor, boost-range, boost-serialization, boost-throw-exception, boost-tuple, boost-type-traits, boost-typeof]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-accumulators-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-accumulators-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-accumulators-
            boost-ccache-
      - name: boost_accumulators and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_accumulators \
            -DCME_PORT_TARGETS=Boost::accumulators \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-atomic:
    needs: [boost-align, boost-assert, boost-config, boost-predef, boost-preprocessor, boost-type-traits, boost-winapi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-atomic-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-atomic-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-atomic-
            boost-ccache-
      - name: boost_atomic and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_atomic \
            -DCME_PORT_TARGETS=Boost::atomic \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-scope-exit:
    needs: [boost-config, boost-function, boost-preprocessor, boost-type-traits, boost-typeof]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-scope-exit-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-scope-exit-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-scope-exit-
            boost-ccache-
      - name: boost_scope_exit and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_scope_exit \
            -DCME_PORT_TARGETS=Boost::scope_exit \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-flyweight:
    needs: [boost-assert, boost-config, boost-container-hash, boost-core, boost-detail, boost-interprocess, boost-mpl, boost-multi-index, boost-parameter, boost-preprocessor, boost-smart-ptr, boost-throw-exception, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-flyweight-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-flyweight-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-flyweight-
            boost-ccache-
      - name: boost_flyweight and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_flyweight \
            -DCME_PORT_TARGETS=Boost::flyweight \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-icl:
    needs: [boost-assert, boost-concept-check, boost-config, boost-container, boost-core, boost-date-time, boost-detail, boost-iterator, boost-move, boost-mpl, boost-range, boost-rational, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-icl-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-icl-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-icl-
            boost-ccache-
      - name: boost_icl and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_icl \
            -DCME_PORT_TARGETS=Boost::icl \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-predef:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-predef-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-predef-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-predef-
            boost-ccache-
      - name: boost_predef and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_predef \
            -DCME_PORT_TARGETS=Boost::predef \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-chrono:
    needs: [boost-assert, boost-config, boost-core, boost-integer, boost-move, boost-mpl, boost-predef, boost-ratio, boost-system, boost-throw-exception, boost-type-traits, boost-typeof, boost-utility, boost-winapi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-chrono-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-chrono-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-chrono-
            boost-ccache-
      - name: boost_chrono and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_chrono \
            -DCME_PORT_TARGETS=Boost::chrono \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-polygon:
    needs: [boost-config]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-polygon-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-polygon-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-polygon-
            boost-ccache-
      - name: boost_polygon and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_polygon \
            -DCME_PORT_TARGETS=Boost::polygon \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-msm:
    needs: [boost-any, boost-assert, boost-bind, boost-circular-buffer, boost-config, boost-core, boost-function, boost-fusion, boost-mp11, boost-mpl, boost-parameter, boost-phoenix, boost-preprocessor, boost-proto, boost-serialization, boost-tuple, boost-type-traits, boost-typeof]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-msm-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-msm-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-msm-
            boost-ccache-
      - name: boost_msm and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_msm \
            -DCME_PORT_TARGETS=Boost::msm \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-heap:
    needs: [boost-assert, boost-concept-check, boost-config, boost-core, boost-intrusive, boost-iterator, boost-parameter, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-heap-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-heap-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-heap-
            boost-ccache-
      - name: boost_heap and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_heap \
            -DCME_PORT_TARGETS=Boost::heap \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-coroutine:
    needs: [boost-assert, boost-config, boost-context, boost-core, boost-exception, boost-move, boost-system, boost-throw-exception, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-coroutine-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-coroutine-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-coroutine-
            boost-ccache-
      - name: boost_coroutine and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_coroutine \
            -DCME_PORT_TARGETS=Boost::coroutine \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-coroutine2:
    needs: [boost-assert, boost-config, boost-context]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-coroutine2-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-coroutine2-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-coroutine2-
            boost-ccache-
      - name: boost_coroutine2 and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_coroutine2 \
            -DCME_PORT_TARGETS=Boost::coroutine2 \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-ratio:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-ratio-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-ratio-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-ratio-
            boost-ccache-
      - name: boost_ratio and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_ratio \
            -DCME_PORT_TARGETS=Boost::ratio \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-numeric-odeint:
    needs: [boost-assert, boost-compute, boost-config, boost-core, boost-fusion, boost-iterator, boost-math, boost-mpi, boost-mpl, boost-multi-array, boost-numeric-ublas, boost-preprocessor, boost-range, boost-throw-exception, boost-type-traits, boost-units, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-numeric-odeint-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-numeric-odeint-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-numeric-odeint-
            boost-ccache-
      - name: boost_numeric_odeint and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_numeric_odeint \
            -DCME_PORT_TARGETS=Boost::numeric_odeint \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-geometry:
    needs: [boost-algorithm, boost-any, boost-array, boost-assert, boost-concept-check, boost-config, boost-container, boost-core, boost-endian, boost-function-types, boost-fusion, boost-graph, boost-headers, boost-integer, boost-iterator, boost-lexical-cast, boost-math, boost-move, boost-mpl, boost-multiprecision, boost-numeric-conversion, boost-polygon, boost-predef, boost-qvm, boost-range, boost-rational, boost-serialization, boost-thread, boost-throw-exception, boost-tokenizer, boost-tuple, boost-utility, boost-variant, boost-variant2]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-geometry-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-geometry-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-geometry-
            boost-ccache-
      - name: boost_geometry and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_geometry \
            -DCME_PORT_TARGETS=Boost::geometry \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-phoenix:
    needs: [boost-assert, boost-bind, boost-config, boost-core, boost-function, boost-fusion, boost-mpl, boost-predef, boost-preprocessor, boost-proto, boost-range, boost-smart-ptr, boost-type-traits, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-phoenix-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-phoenix-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-phoenix-
            boost-ccache-
      - name: boost_phoenix and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_phoenix \
            -DCME_PORT_TARGETS=Boost::phoenix \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-move:
    needs: [boost-config]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-move-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-move-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-move-
            boost-ccache-
      - name: boost_move and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_move \
            -DCME_PORT_TARGETS=Boost::move \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-locale:
    needs: [boost-assert, boost-charconv, boost-config, boost-core, boost-iterator, boost-predef, boost-thread]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-locale-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-locale-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-locale-
            boost-ccache-
      - name: boost_locale and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_locale \
            -DCME_PORT_TARGETS=Boost::locale \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-container:
    needs: [boost-assert, boost-config, boost-intrusive, boost-move]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-container-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-container-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-container-
            boost-ccache-
      - name: boost_container and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_container \
            -DCME_PORT_TARGETS=Boost::container \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-local-function:
    needs: [boost-config, boost-mpl, boost-preprocessor, boost-scope-exit, boost-type-traits, boost-typeof, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-local-function-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-local-function-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-local-function-
            boost-ccache-
      - name: boost_local_function and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_local_function \
            -DCME_PORT_TARGETS=Boost::local_function \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-context:
    needs: [boost-assert, boost-config, boost-core, boost-mp11, boost-pool, boost-predef, boost-smart-ptr]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-context-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-context-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-context-
            boost-ccache-
      - name: boost_context and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_context \
            -DCME_PORT_TARGETS=Boost::context \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-type-erasure:
    needs: [boost-assert, boost-config, boost-core, boost-fusion, boost-iterator, boost-mp11, boost-mpl, boost-preprocessor, boost-smart-ptr, boost-thread, boost-throw-exception, boost-type-traits, boost-typeof, boost-vmd]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-type-erasure-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-type-erasure-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-type-erasure-
            boost-ccache-
      - name: boost_type_erasure and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_type_erasure \
            -DCME_PORT_TARGETS=Boost::type_erasure \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-multiprecision:
    needs: [boost-assert, boost-config, boost-core, boost-integer, boost-lexical-cast, boost-math, boost-random]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-multiprecision-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-multiprecision-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-multiprecision-
            boost-ccache-
      - name: boost_multiprecision and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_multiprecision \
            -DCME_PORT_TARGETS=Boost::multiprecision \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-lockfree:
    needs: [boost-align, boost-assert, boost-atomic, boost-config, boost-core, boost-parameter, boost-predef, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-lockfree-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-lockfree-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-lockfree-
            boost-ccache-
      - name: boost_lockfree and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_lockfree \
            -DCME_PORT_TARGETS=Boost::lockfree \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-assert:
    needs: [boost-config]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-assert-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-assert-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-assert-
            boost-ccache-
      - name: boost_assert and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_assert \
            -DCME_PORT_TARGETS=Boost::assert \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-align:
    needs: [boost-assert, boost-config, boost-core]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-align-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-align-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-align-
            boost-ccache-
      - name: boost_align and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_align \
            -DCME_PORT_TARGETS=Boost::align \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-type-index:
    needs: [boost-config, boost-container-hash, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-type-index-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-type-index-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-type-index-
            boost-ccache-
      - name: boost_type_index and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_type_index \
            -DCME_PORT_TARGETS=Boost::type_index \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-core:
    needs: [boost-assert, boost-config, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-core-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-core-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-core-
            boost-ccache-
      - name: boost_core and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_core \
            -DCME_PORT_TARGETS=Boost::core \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-throw-exception:
    needs: [boost-assert, boost-config]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-throw-exception-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-throw-exception-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-throw-exception-
            boost-ccache-
      - name: boost_throw_exception and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_throw_exception \
            -DCME_PORT_TARGETS=Boost::throw_exception \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-winapi:
    needs: [boost-config, boost-predef]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-winapi-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-winapi-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-winapi-
            boost-ccache-
      - name: boost_winapi and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_winapi \
            -DCME_PORT_TARGETS=Boost::winapi \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-lexical-cast:
    needs: [boost-config, boost-container, boost-core, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-lexical-cast-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-lexical-cast-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-lexical-cast-
            boost-ccache-
      - name: boost_lexical_cast and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_lexical_cast \
            -DCME_PORT_TARGETS=Boost::lexical_cast \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-sort:
    needs: [boost-config, boost-core, boost-range, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-sort-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-sort-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-sort-
            boost-ccache-
      - name: boost_sort and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_sort \
            -DCME_PORT_TARGETS=Boost::sort \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-convert:
    needs: [boost-config, boost-core, boost-function-types, boost-lexical-cast, boost-math, boost-mpl, boost-optional, boost-parameter, boost-range, boost-spirit, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-convert-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-convert-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-convert-
            boost-ccache-
      - name: boost_convert and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_convert \
            -DCME_PORT_TARGETS=Boost::convert \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-endian:
    needs: [boost-config]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-endian-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-endian-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-endian-
            boost-ccache-
      - name: boost_endian and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_endian \
            -DCME_PORT_TARGETS=Boost::endian \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-vmd:
    needs: [boost-preprocessor]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-vmd-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-vmd-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-vmd-
            boost-ccache-
      - name: boost_vmd and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_vmd \
            -DCME_PORT_TARGETS=Boost::vmd \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-dll:
    needs: [boost-assert, boost-config, boost-core, boost-filesystem, boost-predef, boost-system, boost-throw-exception, boost-type-index, boost-winapi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-dll-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-dll-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-dll-
            boost-ccache-
      - name: boost_dll and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_dll \
            -DCME_PORT_TARGETS=Boost::dll \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-compute:
    needs: [boost-algorithm, boost-array, boost-assert, boost-atomic, boost-chrono, boost-config, boost-core, boost-filesystem, boost-function, boost-function-types, boost-fusion, boost-iterator, boost-lexical-cast, boost-mpl, boost-optional, boost-preprocessor, boost-property-tree, boost-proto, boost-range, boost-smart-ptr, boost-thread, boost-throw-exception, boost-tuple, boost-type-traits, boost-typeof, boost-utility, boost-uuid]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-compute-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-compute-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-compute-
            boost-ccache-
      - name: boost_compute and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_compute \
            -DCME_PORT_TARGETS=Boost::compute \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-hana:
    needs: [boost-config, boost-core, boost-fusion, boost-mpl, boost-tuple]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-hana-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-hana-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-hana-
            boost-ccache-
      - name: boost_hana and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_hana \
            -DCME_PORT_TARGETS=Boost::hana \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-metaparse:
    needs: [boost-config, boost-mpl, boost-predef, boost-preprocessor, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-metaparse-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-metaparse-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-metaparse-
            boost-ccache-
      - name: boost_metaparse and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_metaparse \
            -DCME_PORT_TARGETS=Boost::metaparse \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-qvm:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-qvm-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-qvm-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-qvm-
            boost-ccache-
      - name: boost_qvm and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_qvm \
            -DCME_PORT_TARGETS=Boost::qvm \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-fiber:
    needs: [boost-algorithm, boost-assert, boost-config, boost-context, boost-core, boost-filesystem, boost-format, boost-intrusive, boost-predef, boost-smart-ptr]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-fiber-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-fiber-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-fiber-
            boost-ccache-
      - name: boost_fiber and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_fiber \
            -DCME_PORT_TARGETS=Boost::fiber \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-process:
    needs: [boost-algorithm, boost-asio, boost-config, boost-core, boost-filesystem, boost-fusion, boost-iterator, boost-move, boost-optional, boost-system, boost-tokenizer, boost-type-index, boost-winapi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-process-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-process-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-process-
            boost-ccache-
      - name: boost_process and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_process \
            -DCME_PORT_TARGETS=Boost::process \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-stacktrace:
    needs: [boost-config, boost-container-hash, boost-core, boost-predef, boost-winapi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-stacktrace-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-stacktrace-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-stacktrace-
            boost-ccache-
      - name: boost_stacktrace and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_stacktrace \
            -DCME_PORT_TARGETS=Boost::stacktrace \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-poly-collection:
    needs: [boost-assert, boost-config, boost-core, boost-iterator, boost-mp11, boost-mpl, boost-type-erasure, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-poly-collection-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-poly-collection-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-poly-collection-
            boost-ccache-
      - name: boost_poly_collection and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_poly_collection \
            -DCME_PORT_TARGETS=Boost::poly_collection \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-beast:
    needs: [boost-asio, boost-assert, boost-bind, boost-config, boost-container, boost-container-hash, boost-core, boost-endian, boost-headers, boost-intrusive, boost-logic, boost-mp11, boost-optional, boost-smart-ptr, boost-static-string, boost-system, boost-throw-exception, boost-type-index, boost-type-traits, boost-winapi]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-beast-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-beast-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-beast-
            boost-ccache-
      - name: boost_beast and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_beast \
            -DCME_PORT_TARGETS=Boost::beast \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-mp11:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-mp11-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-mp11-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-mp11-
            boost-ccache-
      - name: boost_mp11 and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_mp11 \
            -DCME_PORT_TARGETS=Boost::mp11 \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-callable-traits:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-callable-traits-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-callable-traits-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-callable-traits-
            boost-ccache-
      - name: boost_callable_traits and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_callable_traits \
            -DCME_PORT_TARGETS=Boost::callable_traits \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-contract:
    needs: [boost-any, boost-assert, boost-config, boost-core, boost-exception, boost-function, boost-function-types, boost-mpl, boost-optional, boost-preprocessor, boost-smart-ptr, boost-thread, boost-type-traits, boost-typeof, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-contract-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-contract-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-contract-
            boost-ccache-
      - name: boost_contract and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_contract \
            -DCME_PORT_TARGETS=Boost::contract \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-container-hash:
    needs: [boost-config, boost-describe, boost-mp11]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-container-hash-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-container-hash-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-container-hash-
            boost-ccache-
      - name: boost_container_hash and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_container_hash \
            -DCME_PORT_TARGETS=Boost::container_hash \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-hof:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-hof-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-hof-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-hof-
            boost-ccache-
      - name: boost_hof and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_hof \
            -DCME_PORT_TARGETS=Boost::hof \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-yap:
    needs: [boost-hana, boost-preprocessor, boost-type-index]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-yap-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-yap-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-yap-
            boost-ccache-
      - name: boost_yap and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_yap \
            -DCME_PORT_TARGETS=Boost::yap \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-safe-numerics:
    needs: [boost-concept-check, boost-config, boost-core, boost-integer, boost-logic, boost-mp11]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-safe-numerics-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-safe-numerics-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-safe-numerics-
            boost-ccache-
      - name: boost_safe_numerics and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_safe_numerics \
            -DCME_PORT_TARGETS=Boost::safe_numerics \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-parameter-python:
    needs: [boost-mpl, boost-parameter, boost-preprocessor, boost-python]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-parameter-python-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-parameter-python-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-parameter-python-
            boost-ccache-
      - name: boost_parameter_python and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_parameter_python \
            -DCME_PORT_TARGETS=Boost::parameter_python \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-headers:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-headers-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-headers-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-headers-
            boost-ccache-
      - name: boost_headers and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_headers \
            -DCME_PORT_TARGETS=Boost::headers \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-outcome:
    needs: [boost-config, boost-exception, boost-headers, boost-system, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-outcome-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-outcome-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-outcome-
            boost-ccache-
      - name: boost_outcome and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_outcome \
            -DCME_PORT_TARGETS=Boost::outcome \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-histogram:
    needs: [boost-config, boost-core, boost-mp11, boost-throw-exception, boost-variant2]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-histogram-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-histogram-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-histogram-
            boost-ccache-
      - name: boost_histogram and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_histogram \
            -DCME_PORT_TARGETS=Boost::histogram \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-variant2:
    needs: [boost-assert, boost-config, boost-mp11]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-variant2-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-variant2-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-variant2-
            boost-ccache-
      - name: boost_variant2 and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_variant2 \
            -DCME_PORT_TARGETS=Boost::variant2 \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-nowide:
    needs: [boost-config]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-nowide-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-nowide-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-nowide-
            boost-ccache-
      - name: boost_nowide and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_nowide \
            -DCME_PORT_TARGETS=Boost::nowide \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-static-string:
    needs: [boost-assert, boost-container-hash, boost-core, boost-headers, boost-throw-exception, boost-utility]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-static-string-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-static-string-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-static-string-
            boost-ccache-
      - name: boost_static_string and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_static_string \
            -DCME_PORT_TARGETS=Boost::static_string \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-stl-interfaces:
    needs: [boost-assert, boost-config]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-stl-interfaces-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-stl-interfaces-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-stl-interfaces-
            boost-ccache-
      - name: boost_stl_interfaces and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_stl_interfaces \
            -DCME_PORT_TARGETS=Boost::stl_interfaces \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-leaf:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-leaf-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-leaf-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-leaf-
            boost-ccache-
      - name: boost_leaf and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_leaf \
            -DCME_PORT_TARGETS=Boost::leaf \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-json:
    needs: [boost-assert, boost-config, boost-container, boost-container-hash, boost-core, boost-describe, boost-endian, boost-mp11, boost-system, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-json-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-json-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-json-
            boost-ccache-
      - name: boost_json and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_json \
            -DCME_PORT_TARGETS=Boost::json \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-pfr:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-pfr-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-pfr-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-pfr-
            boost-ccache-
      - name: boost_pfr and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_pfr \
            -DCME_PORT_TARGETS=Boost::pfr \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-describe:
    needs: [boost-mp11]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-describe-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-describe-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-describe-
            boost-ccache-
      - name: boost_describe and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_describe \
            -DCME_PORT_TARGETS=Boost::describe \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-lambda2:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-lambda2-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-lambda2-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-lambda2-
            boost-ccache-
      - name: boost_lambda2 and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_lambda2 \
            -DCME_PORT_TARGETS=Boost::lambda2 \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-property-map-parallel:
    needs: [boost-assert, boost-bind, boost-concept-check, boost-config, boost-function, boost-mpi, boost-mpl, boost-multi-index, boost-optional, boost-property-map, boost-serialization, boost-smart-ptr, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-property-map-parallel-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-property-map-parallel-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-property-map-parallel-
            boost-ccache-
      - name: boost_property_map_parallel and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_property_map_parallel \
            -DCME_PORT_TARGETS=Boost::property_map_parallel \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-url:
    needs: [boost-align, boost-assert, boost-config, boost-core, boost-headers, boost-mp11, boost-optional, boost-system, boost-throw-exception, boost-type-traits, boost-variant2]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-url-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-url-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-url-
            boost-ccache-
      - name: boost_url and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_url \
            -DCME_PORT_TARGETS=Boost::url \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-mysql:
    needs: [boost-asio, boost-assert, boost-charconv, boost-compat, boost-config, boost-container, boost-core, boost-describe, boost-endian, boost-intrusive, boost-mp11, boost-optional, boost-system, boost-throw-exception, boost-variant2]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-mysql-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-mysql-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-mysql-
            boost-ccache-
      - name: boost_mysql and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_mysql \
            -DCME_PORT_TARGETS=Boost::mysql \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-compat:
    needs: [boost-assert, boost-config, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-compat-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-compat-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-compat-
            boost-ccache-
      - name: boost_compat and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_compat \
            -DCME_PORT_TARGETS=Boost::compat \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-redis:
    needs: [boost-asio, boost-assert, boost-core, boost-mp11, boost-system, boost-throw-exception]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-redis-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-redis-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-redis-
            boost-ccache-
      - name: boost_redis and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_redis \
            -DCME_PORT_TARGETS=Boost::redis \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-cobalt:
    needs: [boost-asio, boost-callable-traits, boost-circular-buffer, boost-config, boost-container, boost-core, boost-endian, boost-intrusive, boost-mp11, boost-preprocessor, boost-smart-ptr, boost-static-string, boost-system, boost-throw-exception, boost-variant2]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-cobalt-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-cobalt-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-cobalt-
            boost-ccache-
      - name: boost_cobalt and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_cobalt \
            -DCME_PORT_TARGETS=Boost::cobalt \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-charconv:
    needs: [boost-assert, boost-config, boost-core]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-charconv-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-charconv-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-charconv-
            boost-ccache-
      - name: boost_charconv and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_charconv \
            -DCME_PORT_TARGETS=Boost::charconv \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-scope:
    needs: [boost-config, boost-core, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-scope-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-scope-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-scope-
            boost-ccache-
      - name: boost_scope and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_scope \
            -DCME_PORT_TARGETS=Boost::scope \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-parser:
    needs: [boost-assert, boost-hana, boost-type-index]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-parser-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-parser-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-parser-
            boost-ccache-
      - name: boost_parser and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_parser \
            -DCME_PORT_TARGETS=Boost::parser \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-mqtt5:
    needs: [boost-asio, boost-assert, boost-container, boost-core, boost-endian, boost-headers, boost-random, boost-range, boost-smart-ptr, boost-system, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-mqtt5-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-mqtt5-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-mqtt5-
            boost-ccache-
      - name: boost_mqtt5 and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_mqtt5 \
            -DCME_PORT_TARGETS=Boost::mqtt5 \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-hash2:
    needs: [boost-assert, boost-config, boost-container-hash, boost-describe, boost-mp11]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-hash2-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-hash2-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-hash2-
            boost-ccache-
      - name: boost_hash2 and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_hash2 \
            -DCME_PORT_TARGETS=Boost::hash2 \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-bloom:
    needs: [boost-assert, boost-config, boost-container-hash, boost-core, boost-throw-exception, boost-type-traits]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-bloom-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-bloom-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-bloom-
            boost-ccache-
      - name: boost_bloom and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_bloom \
            -DCME_PORT_TARGETS=Boost::bloom \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-openmethod:
    needs: [boost-assert, boost-config, boost-core, boost-dynamic-bitset, boost-headers, boost-mp11, boost-preprocessor, boost-smart-ptr]
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-openmethod-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-openmethod-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-openmethod-
            boost-ccache-
      - name: boost_openmethod and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_openmethod \
            -DCME_PORT_TARGETS=Boost::openmethod \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
  boost-decimal:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: sudo apt-get update && sudo apt-get install -y ninja-build ccache
      - uses: actions/cache@v4
        with:
          path: .cache/sources
          key: boost-git-boost-decimal-1.92.0
          restore-keys: boost-git-
      - uses: actions/cache@v4
        with:
          path: .cache/ccache
          key: boost-ccache-boost-decimal-${{ github.sha }}
          restore-keys: |
            boost-ccache-boost-decimal-
            boost-ccache-
      - name: boost_decimal and what it is built on
        run: |
          export CPM_SOURCE_CACHE="$PWD/.cache/sources"
          export CCACHE_DIR="$PWD/.cache/ccache"
          tools/configure -S test/port -B build/one -G Ninja \
            -DCMAKE_PROJECT_TOP_LEVEL_INCLUDES="$PWD/cmake-everywhere.cmake" \
            -DCME_SYSTEM=NEVER -DCME_STORE= -DCME_LOCK= \
            -DCME_PORT_PACKAGE=boost_decimal \
            -DCME_PORT_TARGETS=Boost::decimal \
            -DCMAKE_C_COMPILER_LAUNCHER=ccache \
            -DCMAKE_CXX_COMPILER_LAUNCHER=ccache
          cmake --build build/one
          ./build/one/cme-port
