# Nothing beyond what is asked for, and the system's copy wherever there is
# one. A build that wants to be small and does not mind depending on what is
# installed.
set(CME_SYSTEM "AUTO" CACHE STRING "" FORCE)
set(CME_DEFAULT_FEATURES "" CACHE STRING "" FORCE)
