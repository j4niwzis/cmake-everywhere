/* minimp3 is a header that carries its own implementation behind a macro.
   Defining it in exactly one translation unit is what turns the header into
   a library, and this is that translation unit. */
#define MINIMP3_IMPLEMENTATION
#include "minimp3_ex.h"
